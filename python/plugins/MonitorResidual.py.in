
import sys
import pluginsS${DIM} as pluginsS


# Wrapper around the PluginMonitorResidual class
class MonitorResidual(pluginsS.MonitorResidual):

	# Create a new instance
	# @param[in] app	the Maci application instance
	def __init__(self, app, **kwargs):
		pluginsS.MonitorResidual.__init__(self, app, kwargs["outfile"]);

