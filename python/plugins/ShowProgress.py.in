
import sys
import pluginsS${DIM} as pluginsS


# Wrapper around the ShowProgress class
class ShowProgress(pluginsS.ShowProgress):

	# Create a new instance
	# @param[in] app	the Maci application instance
	def __init__(self, app, **kwargs):
		pluginsS.ShowProgress.__init__(self, app, kwargs["mask"]);

