
import sys
import pluginsS${DIM} as pluginsS


# Wrapper around the PluginVisualize class
class Visualize(pluginsS.Visualize):

	# Create a new instance
	# @param[in] app	the Maci application instance
	def __init__(self, app, **kwargs):
		pluginsS.Visualize.__init__(self, app, kwargs["mask"], kwargs["prefix"]);

